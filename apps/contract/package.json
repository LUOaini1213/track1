{
  "name": "@launchpad/contract",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Wire types and pure logic shared by the control plane and the Playground.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.0.15"
  },
  "main": "./dist/index.js"
}
